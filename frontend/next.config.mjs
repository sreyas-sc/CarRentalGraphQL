/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['stimg.cardekho.com','imgd.aeplcdn.com'] // Add the hostname here
      },
      // other configurations if any
};

export default nextConfig;
